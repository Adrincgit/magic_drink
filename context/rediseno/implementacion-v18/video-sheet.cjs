const sharp=require('sharp'),fs=require('node:fs'),path=require('node:path');
(async()=>{const dir=path.join(__dirname,'capturas');const frames=fs.readdirSync(dir).filter(n=>/^frame-\d+\.png$/.test(n));for(let part=0;part<2;part++){const list=frames.slice(part*12,part*12+12);await sharp({create:{width:1920,height:Math.ceil(list.length/4)*324,channels:3,background:'#26182e'}}).composite(await Promise.all(list.map(async(n,i)=>({input:await sharp(path.join(dir,n)).resize(480,300).extend({bottom:24,background:'#26182e'}).composite([{input:Buffer.from('<svg width="480" height="324"><text x="12" y="318" fill="white" font-size="16">'+n+'</text></svg>')}]).png().toBuffer(),left:(i%4)*480,top:Math.floor(i/4)*324})))).png().toFile(path.join(dir,'walk-sheet-'+part+'.png'));}})()
